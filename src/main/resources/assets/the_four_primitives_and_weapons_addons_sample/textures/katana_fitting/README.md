# Katana fitting textures

拵えは `tsuba`（鍔）、`tsuka`（柄）、`kasira`（頭）、`fuchi`（縁）に分けます。

`models/custom/weapon/katana/sample_katana_parent.json` のテクスチャ番号は次の対応です。

- `0`: 刀身
- `1`: 鍔
- `3`: 頭
- `4`: 柄

独自テクスチャを使う場合は、たとえば `katana_fitting/tsuba/sample_tsuba.png` を置き、モデルの `1` をアドオンの名前空間へ変更してください。
